services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: lexora
      POSTGRES_USER: lexora
      POSTGRES_PASSWORD: lexora_dev_only
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - lexora_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lexora -d lexora"]
      interval: 5s
      timeout: 3s
      retries: 20

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - lexora_redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

volumes:
  lexora_postgres:
  lexora_redis:
